package enemies;

import static helpz.Constants.Enemies.EXTRATER;

import managers.EnemyManager;

public class ExtraTer extends AEnemy{

    public ExtraTer(float x, float y, int ID, EnemyManager em) {
        super(x, y, ID, EXTRATER, em);
    }
    
}
